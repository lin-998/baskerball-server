const userModel=require('../models/sql_user')
const {uuid}=require('../utils/createId')
/**
 * @param   time  时间
 * @return
 */
class userController{
	  async createPost(ctx, next) {
		
   };
  
}
module.exports=new userController()
